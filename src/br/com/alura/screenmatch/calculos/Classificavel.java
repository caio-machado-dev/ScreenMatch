package br.com.alura.screenmatch.calculos;

// A interface é basicamente um "Contrato" que exige que a classe que utilizar contenha os métodos e atributos da interface
public interface Classificavel {
    int getClassificacao();
}